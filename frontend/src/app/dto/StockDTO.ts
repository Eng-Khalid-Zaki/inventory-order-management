export interface StockDTO {
    productId: number;
    quantity: number;
    storeId: number;
}

export interface StockModel {
    productId: number;
    productName: string;
    quantity: number;
    storeId: number;
    storeName: string;
}